{% extends 'base.html.twig' %}

{% block title %}Ajouter un utilisateur{% endblock %}

{% block body %}

    {# NAV BACK #}
			<nav class="bike-detail__back">
				<img src="{{asset('icons/authentification/arrow_left.svg')}}" width="16" height="16" alt="" aria-hidden="true">
				<a class="auth-card__link font-medium" href="{{ path('app_user_index') }}">Retourner voir la liste des utilisateurs</a>
			</nav>
    {# HEADER #}
	<header class="dashboard">
		<div class="dashboard__wrapper">
			<div class="dashboard__title">
				<img class="dashboard__icon" src="{{asset('icons/garage/biker_on_bike_red_dashboard_garage.svg')}}" alt="" aria-hidden="true">
				<h1>Ajouter un utilisateur</h1>
			</div>
			<p class="text-xs">Créer un nouvel utilisateur</p>
		</div>
	</header>

    {# FORM #}
    {{ include('user/_form.html.twig') }}

    {# <a href="{{ path('app_user_index') }}">back to list</a> #}
{% endblock %}
