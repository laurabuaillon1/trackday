<form method="post" action="{{ path('app_user_delete',{'id': user.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet utilisateur');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete'~ user.id) }}">
    <button class="card__button btn-primary" type="submit">
		{# <img class="dashboard__icon" src="{{asset('icons/compte/trash_white.svg')}}" alt="" aria-hidden="true"> #}
		Supprimer l'utilisateur
	</button>
</form>
