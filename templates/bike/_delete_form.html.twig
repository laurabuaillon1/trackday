<form method="post" action="{{ path('app_bike_delete', {'id': bike.id}) }}" onsubmit="Êtes-vous sûr de vouloir supprimer cette moto ?">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bike.id) }}">
    <button class="btn simple-btn">Supprimer</button>
</form>
